# Stub for the plotting module referenced by learning/learning_dev_f.py.
# The full implementation is not present in this repo; only the PlotElement
# class is needed, and only inside the video_plotter code path which is
# never triggered when video_plotter=None (the default in all our calls).

class PlotElement:
    def __init__(self, *args, **kwargs):
        pass
